name = "zpipe-example"
mode = "driver"
flavor = "gnu"
entry = "zpipe.c"
include_dirs = ["include"]
allow_system_includes = true
expected = "parse_ok"
source = "https://github.com/madler/zlib"
upstream_ref = "v1.3.1"
license = "public-domain"
notes = "Official zlib example program with local zlib headers and explicit host libc include opt-in."
tags = ["external", "example", "zlib", "slow"]
